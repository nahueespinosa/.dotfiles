[defaults]
# Use current working directory for role discovery
roles_path = ./ansible/roles

# Reduce verbosity for cleaner output (use -v or -vv for details)
deprecation_warnings = False

# Force color output
force_color = True

# Don't create .retry files
retry_files_enabled = False
